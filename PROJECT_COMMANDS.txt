Reduce PowerShell prompt:
Function Prompt { "$( (Get-Location | Split-Path -Leaf) )> " }

Access PostgreSQL inside the Docker container:
docker exec -it postgres psql -U postgres

View Producer 1 logs:
sudo journalctl -u kafka-pihole-producer -f

Run local log testing scripts:
python -m tests.test_pihole_log_reader
python -m tests.test_pihole_log_api

Pi-hole log file:
/var/log/pihole/pihole.log

Start Kafka container:
docker start kafka

Stop and start all containers:
docker-compose down
docker-compose up -d

Run a Kafka consumer inside Docker:
docker exec -it kafka bash
/opt/kafka/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic pi-hole.logs.file --from-beginning

Run Consumer 1 locally:
python -m src.consumers.consumer_1_logs --test
python -m src.consumers.consumer_1_logs

---

Run Producer 2 locally:
python -m src.producers.producer_2_api_logs
python -m src.producers.producer_2_api_logs --test

Run Consumer 2 locally:
python -m src.consumers.consumer_2_api_logs
python -m src.consumers.consumer_2_api_logs --test

---

Run Producer 3 locally:
python -m src.producers.producer_3_api_ideas
python -m src.producers.producer_3_api_ideas --test

Run Consumer 3 locally:
python -m src.consumers.consumer_3_api_ideas
python -m src.consumers.consumer_3_api_ideas --test

---

Run Producer 4 locally:
python -m src.producers.producer_4_public_apis
python -m src.producers.producer_4_public_apis --test

Run Consumer 4 locally:
python -m src.consumers.consumer_4_public_apis
python -m src.consumers.consumer_4_public_apis --test

---

Run Producer 5 locally:
python -m src.producers.producer_5_faker
python -m src.producers.producer_5_faker --test

Run Consumer 5 locally:
python -m src.consumers.consumer_5_faker
python -m src.consumers.consumer_5_faker --test

---

Kafka UI:
http://localhost:8080/

Verify whether Kafka is running in KRaft mode:
docker exec -it kafka bash -c "ls /opt/kafka/bin | grep kraft"